{
  "name": "the-shoppies",
  "version": "0.1.0",
  "main": "index.js",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  },
  "scripts": {
    "start": "node index.js",
    "start-dev": "nodemon index.js",
    "test": "npx jest --forceExit tests/*",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "nock": "^13.0.4",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  }
}
